# LanceDB 大规模数据混合检索性能优化报告

> **数据规模**：41,488,110 条 Wikipedia 文档（去重后）
> **向量维度**：1024 维
> **索引类型**：IVF_PQ（16384 个分区）+ Tantivy FTS 全文索引
> **优化日期**：2026-04-15

---

## 一、优化前的现象

### 1.1 问题描述

在对 LanceDB 进行大规模数据（4100 万条）压测时，混合检索（Hybrid Search）出现了严重的性能退化问题：

- **FTS（全文检索）**：p50 ≈ 19ms，QPS ≈ 510 ✅ 正常
- **Vector（向量检索）**：p50 ≈ 51ms，QPS ≈ 317 ✅ 正常
- **Hybrid（混合检索）**：p50 ≈ **2957ms**，QPS ≈ **1.3** ❌ 严重异常

混合检索的延迟是 FTS 的 **155 倍**，是 Vector 的 **58 倍**。理论上混合检索只是 FTS + Vector 的组合加上结果融合，不应该有如此巨大的性能差距。

### 1.2 初始压测结果

| 检索类型 | 查询数 | 轮次 | QPS | p50 (ms) | p95 (ms) | p99 (ms) | 并发 |
|---------|-------|------|-----|----------|----------|----------|------|
| fts | 1000 | 3 | 497.0 | 20.9 | 76.8 | 284.0 | 16 |
| vector | 1000 | 3 | 289.0 | 47.2 | 81.0 | 116.7 | 16 |
| hybrid | 1000 | 3 | **1.3** | **2957.6** | **4200+** | **5000+** | 4 |

### 1.3 伴随问题：OOM（内存溢出）

在早期压测中，混合检索还频繁触发 OOM Killer：

- 进程内存从初始 ~267MB 暴涨到 **24.7GB+**
- 被 Linux cgroup OOM Killer 强制终止
- 错误日志：`Memory cgroup out of memory: Killed process (python3)`

---

## 二、根因分析

### 2.1 排除法定位

我们通过一系列对照实验，逐步排除了可能的原因：

| 假设 | 验证方式 | 结论 |
|------|---------|------|
| 混合检索本身就慢？ | 在干净进程中单独测试 Hybrid | ❌ 排除。干净进程中 p50=27.5ms |
| 并发过高导致资源争用？ | 降低并发到 4 | ❌ 排除。降低并发后仍然 ~3000ms |
| GC 导致的停顿？ | 分析 GC 时机 | ❌ 排除。GC 只在 trial 间执行 |
| 分批策略引入的开销？ | 对比分批 vs 不分批 | ❌ 排除。独立测试分批模式正常 |
| **前序查询的缓存污染？** | 先跑 Vector 3000 次再跑 Hybrid | ✅ **确认！** |

### 2.2 关键实验数据

#### 实验 1：干净进程直接跑 Hybrid

```
串行 50 次: p50=26.0ms, avg=33.0ms
并发4 × 50次: p50=25.0ms, avg=35.0ms
并发16 × 50次: p50=60.0ms, avg=55.0ms
```

**结论**：混合检索本身性能完全正常。

#### 实验 2：先跑 Vector 3000 次，再跑 Hybrid（同一连接）

```
Vector 前内存: 267MB
Vector 后内存: 2615MB (增长 2.3GB)
Vector后跑Hybrid 50次: p50=5040ms, avg=2978ms
```

**结论**：Vector 查询累积的缓存导致 Hybrid 性能退化 **83 倍**。

#### 实验 3：先跑 Vector 3000 次，关闭连接重连，再跑 Hybrid

```
重新连接后Hybrid 50次: p50=27.5ms, avg=36.1ms
```

**结论**：关闭连接释放 Rust 缓存后，Hybrid 性能完全恢复。

### 2.3 根因：Rust/Lance 引擎内部缓存累积

```
┌─────────────────────────────────────────────────────────────────┐
│                    压测脚本执行流程                               │
│                                                                 │
│  ┌──────────┐    ┌──────────────┐    ┌──────────────┐          │
│  │ FTS ×3000│───▶│ Vector ×3000 │───▶│ Hybrid ×3000 │          │
│  │  正常     │    │ nprobes=128  │    │  严重退化！    │          │
│  └──────────┘    └──────┬───────┘    └──────────────┘          │
│                         │                                       │
│                         ▼                                       │
│              ┌─────────────────────┐                            │
│              │ Rust/Lance 引擎     │                            │
│              │ 缓存 128 个 IVF 分区 │                            │
│              │ × 3000 次查询       │                            │
│              │ = 2.3GB 内部缓存    │                            │
│              └─────────────────────┘                            │
│                         │                                       │
│                         ▼                                       │
│              ┌─────────────────────┐                            │
│              │ 内存压力 + 缓存争用  │                            │
│              │ Hybrid 需要同时做    │                            │
│              │ Vector + FTS 两路   │                            │
│              │ 但内存已被占满       │                            │
│              └─────────────────────┘                            │
└─────────────────────────────────────────────────────────────────┘
```

**具体原因链路**：

1. **Vector 查询使用 `nprobes=128`**：在 16384 个 IVF 分区中扫描 128 个分区，每次查询都将分区数据加载到 Rust 堆内存
2. **3000 次 Vector 查询累积了约 2.3GB 的 Rust 端缓存**：这些缓存位于 Rust 堆中，不受 Python `gc.collect()` 管理
3. **Hybrid 查询内部也需要执行 Vector 搜索**：但此时 Rust 引擎的内存已被之前的缓存占满，导致严重的内存压力和缓存争用
4. **三种检索类型共用同一个 `db` 连接和 `table` 对象**：Rust 端的缓存在连接生命周期内持续累积，无法释放

### 2.4 为什么只有 Hybrid 受影响？

| 检索类型 | 执行顺序 | 前序缓存量 | 影响 |
|---------|---------|-----------|------|
| FTS | 第 1 个 | 0 | 无影响 |
| Vector | 第 2 个 | FTS 缓存（较小） | 轻微影响 |
| **Hybrid** | **第 3 个** | **FTS + Vector 缓存（2.3GB+）** | **严重退化** |

Hybrid 排在最后执行，承受了前两种检索类型累积的全部缓存压力。

---

## 三、优化方案

### 3.1 核心修改：每种检索类型使用独立的数据库连接

**修改前**（问题代码）：

```python
# 三种检索类型共用同一个连接
db = await lancedb.connect_async(str(db_uri))
table = await db.open_table("wikipedia_dedup")

for search_type in SEARCH_TYPES:  # fts → vector → hybrid
    # 全部使用同一个 table 和 db
    ...

db.close()  # 最后才关闭
```

**修改后**（优化代码）：

```python
# 先连接一次，抽取向量数据
db = await lancedb.connect_async(db_uri)
table = await db.open_table("wikipedia_dedup")
# ... 抽取向量 ...
db.close()       # 关闭初始连接
del table, db
gc.collect()

for search_type in SEARCH_TYPES:
    # 每种检索类型创建独立的数据库连接
    print(f"为 {search_type} 创建新的数据库连接...")
    db = await lancedb.connect_async(db_uri)
    table = await db.open_table("wikipedia_dedup")

    # ... 执行压测 ...

    # 完成后关闭连接，释放 Rust 端缓存
    db.close()
    del table, db
    gc.collect()
    print(f"{search_type} 连接已关闭，Rust 缓存已释放。")
```

### 3.2 辅助优化措施

在定位到根因之前，我们还实施了以下辅助优化，这些措施在最终方案中保留：

| 优化项 | 修改内容 | 目的 |
|--------|---------|------|
| 降低 Hybrid nprobes | 128 → 32 | 减少每次查询的分区扫描量，降低内存占用 |
| Hybrid 独立并发控制 | 并发从 16 降到 4 | 避免多个 Hybrid 查询同时占用大量内存 |
| 分批执行 | 每批 50 个查询 | 避免一次性 `asyncio.gather` 1000 个协程 |
| 实时进度日志 | 每批输出进度 | 便于监控 Hybrid 执行速度 |
| 每轮 trial 汇总 | 输出 QPS/p50/p99 | 便于观察性能趋势 |

---

## 四、优化后效果

### 4.1 最终压测结果

| 检索类型 | 查询数 | 轮次 | QPS | p50 (ms) | p95 (ms) | p99 (ms) | 并发 |
|---------|-------|------|-----|----------|----------|----------|------|
| fts | 1000 | 3 | 509.88 | 19.26 | 76.78 | 284.01 | 16 |
| vector | 1000 | 3 | 316.60 | 50.88 | 80.95 | 116.69 | 16 |
| **hybrid** | **1000** | **3** | **118.92** | **22.33** | **77.70** | **109.30** | **4** |

### 4.2 Hybrid 检索优化前后对比

| 指标 | 优化前 | 优化后 | 提升倍数 |
|------|--------|--------|---------|
| **p50 延迟** | 2957 ms | **22.3 ms** | **133×** |
| **p99 延迟** | ~5000 ms | **109.3 ms** | **46×** |
| **QPS** | 1.3 | **118.9** | **91×** |
| **单轮耗时** | ~770s (12min+) | **8.4s** | **92×** |
| **内存峰值** | 24.7 GB (OOM) | **稳定可控** | 不再 OOM |

### 4.3 三轮 Hybrid 压测详细数据

```
[hybrid trial 1/3] 完成 | 耗时 8.47s | QPS 118.1 | p50 22.3ms | p99 108.0ms
[hybrid trial 2/3] 完成 | 耗时 8.35s | QPS 119.8 | p50 22.2ms | p99 109.5ms
[hybrid trial 3/3] 完成 | 耗时 8.41s | QPS 118.8 | p50 22.4ms | p99 110.3ms
```

三轮结果高度一致，说明性能稳定，不存在退化趋势。

### 4.4 一个有趣的发现

优化后 Hybrid 的 **p50=22.3ms** 甚至比单独 Vector 的 **p50=50.88ms** 还快。原因是：

- Hybrid 使用 `nprobes=32`（扫描较少分区），Vector 使用 `nprobes=128`（扫描更多分区）
- Hybrid 并发度为 4，资源争用更少；Vector 并发度为 16

这说明在实际应用中，混合检索完全可以在保证召回质量的前提下，通过合理的 nprobes 配置获得优秀的延迟表现。

---

## 五、经验总结

### 5.1 核心教训

> **在 LanceDB 中，长期复用同一个数据库连接进行大量查询，会导致 Rust/Lance 引擎内部缓存持续累积，最终引发后续查询的严重性能退化甚至 OOM。**

### 5.2 最佳实践建议

1. **隔离不同类型的查询连接**：不同检索类型（FTS、Vector、Hybrid）应使用独立的数据库连接，避免缓存交叉污染
2. **定期重建连接**：在长时间运行的服务中，考虑定期关闭并重建 LanceDB 连接，释放 Rust 端累积的缓存
3. **合理设置 nprobes**：nprobes 不是越大越好，需要在召回率和性能之间权衡。对于 4100 万条数据，nprobes=32 已经能提供良好的召回质量
4. **监控进程内存**：LanceDB 的内存增长主要在 Rust 端，Python 层的 `gc.collect()` 无法释放，需要通过关闭连接来回收

### 5.3 问题定位方法论

本次优化采用了**对照实验法**，通过逐步控制变量来定位根因：

```
干净进程 Hybrid → 正常 (27ms)
     ↓ 对比
同进程先跑 Vector 再跑 Hybrid → 异常 (2978ms)
     ↓ 对比
先跑 Vector → 关闭连接重连 → 再跑 Hybrid → 正常 (27ms)
     ↓ 结论
问题在于 Rust 端缓存累积，关闭连接可释放
```

这种方法比直接猜测和修改更高效，能精确定位到根因而非表面症状。
